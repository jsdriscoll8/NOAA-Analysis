#include <stdio.h>
#include <datastructs.h>

int main() {
    // Test day struct
    char testDate[DATELEN] = "2023-08-30";
    char **columnNames = malloc(2 * sizeof(char *));

    free(column1);
    free(columnNames);
    return 0;
}
